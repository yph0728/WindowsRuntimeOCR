using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;
using Windows.Storage;
using Windows.Storage.Streams;

namespace OCR.ImageParser
{
    public class ImageConverter
    {
        //public async Task<string> ParseImageAsync(StorageFile file) {
        //    string parsedText = "";

        //    try
        //    {
        //        parsedText = await ExtractTextAsync(file);
        //    }
        //    catch {

        //    }
        //    return parsedText;
        //}

        //private async Task<string> ExtractTextAsync(StorageFile file)
        //{
        //    string text = "";

        //    var imageProperties = file.Properties.GetImagePropertiesAsync();

        //    using (IRandomAccessStream imageStream = await file.OpenAsync(FileAccessMode.Read)) {

                

        //    }
        //}
    }
}
